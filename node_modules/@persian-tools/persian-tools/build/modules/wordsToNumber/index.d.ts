export interface WordsToNumberOptions {
    digits?: "en" | "fa" | "ar";
    addCommas?: boolean;
    fuzzy?: boolean;
}
export default function wordsToNumber<TResult extends string | number>(words: string, { digits, addCommas: shouldAddCommas, fuzzy: isEnabledFuzzy }?: WordsToNumberOptions): TResult;
